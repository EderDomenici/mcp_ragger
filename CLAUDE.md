# LangChain RAG

Whenever the user asks about LangChain, LangGraph, or LangSmith — concepts, APIs, usage, or configuration — you MUST call `search_docs` before responding. Do not answer from training knowledge alone.
